@model BankOfBIT.Models.NextInvestmentAccount

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>NextInvestmentAccount</legend>

        @Html.HiddenFor(model => model.NextInvestmentAccountId)

        <div class="editor-label">
            @Html.LabelFor(model => model.NextAvailableNumber)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NextAvailableNumber)
            @Html.ValidationMessageFor(model => model.NextAvailableNumber)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
