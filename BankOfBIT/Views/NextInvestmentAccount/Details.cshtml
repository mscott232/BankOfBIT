@model BankOfBIT.Models.NextInvestmentAccount

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>NextInvestmentAccount</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.NextAvailableNumber)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NextAvailableNumber)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.NextInvestmentAccountId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
